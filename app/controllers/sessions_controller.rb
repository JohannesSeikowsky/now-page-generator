class SessionsController < ApplicationController
  def login
    # check wether email, password if
    # if so log in,
    # if not, send back with apporpriate notice.
  end

  def logout
    # literally means destroying cookie implemented log in.
  end
end
